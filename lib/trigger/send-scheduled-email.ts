// Stub for open-source build: a consumer imports
// `sendUpgradeOneMonthCheckinEmailTask` from this path, but the task is actually
// defined in ./send-upgrade-checkin-email. Re-export it so the import resolves.
export { sendUpgradeOneMonthCheckinEmailTask } from "@/lib/trigger/send-upgrade-checkin-email";
